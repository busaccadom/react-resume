import React from 'react';
import { HomeWrapper } from './Home.styled';

const Home = () => (
 <HomeWrapper data-testid="Home">
    Home Component
 </HomeWrapper>
);

Home.propTypes = {};

Home.defaultProps = {};

export default Home;
